<Page
    x:Class="CodexTweaks.Windows.Pages.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="Transparent">

    <Grid
        MaxWidth="{StaticResource CodexContentMaxWidth}"
        Padding="{StaticResource CodexPagePaddingThickness}"
        HorizontalAlignment="Stretch"
        RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid ColumnSpacing="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Style="{StaticResource CodexPageHeaderStyle}">
                <TextBlock x:Name="PageTitle" Style="{StaticResource CodexPageTitleTextStyle}" />
                <TextBlock x:Name="PageSubtitle" Style="{StaticResource CodexPageSubtitleTextStyle}" />
            </StackPanel>
            <CommandBar
                Grid.Column="1"
                VerticalAlignment="Center"
                Background="Transparent"
                DefaultLabelPosition="Right"
                IsDynamicOverflowEnabled="True">
                <CommandBar.Resources>
                    <ResourceDictionary>
                        <ResourceDictionary.MergedDictionaries>
                            <ResourceDictionary Source="ms-appx:///Microsoft.UI.Xaml/DensityStyles/Compact.xaml" />
                        </ResourceDictionary.MergedDictionaries>
                    </ResourceDictionary>
                </CommandBar.Resources>
                <AppBarButton x:Name="RefreshButton" Click="RefreshButton_Click" Icon="Refresh" />
                <AppBarButton x:Name="OpenFileButton" Click="OpenFileButton_Click" Icon="OpenFile" />
                <AppBarSeparator />
                <AppBarButton x:Name="ClearButton" Click="ClearButton_Click" Icon="Delete" />
            </CommandBar>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource CodexDividerStyle}" />

        <Border
            Grid.Row="2"
            MinHeight="360"
            Padding="0"
            Style="{StaticResource CodexCardStyle}">
            <TextBox
                x:Name="LogTextBox"
                Padding="14"
                AcceptsReturn="True"
                Background="Transparent"
                BorderThickness="0"
                FontFamily="{StaticResource CodexMonospaceFontFamily}"
                FontSize="11"
                HorizontalAlignment="Stretch"
                IsReadOnly="True"
                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                ScrollViewer.VerticalScrollBarVisibility="Visible"
                TextWrapping="NoWrap" />
        </Border>

        <Border Grid.Row="3" Style="{StaticResource CodexDividerStyle}" />

        <TextBlock
            x:Name="LogPathText"
            Grid.Row="4"
            VerticalAlignment="Center"
            Style="{StaticResource CodexMonospaceTextStyle}"
            TextTrimming="CharacterEllipsis" />
    </Grid>
</Page>
